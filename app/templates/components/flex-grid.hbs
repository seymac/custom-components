<thead>
  <tr>
  {{#each column in columnProperties}}
      <td>
        {{column.header}}
      </td>
  {{/each}}
  </tr>
</thead>
<tbody>
  {{#each dat in dataSource}}
    <tr>
      {{#each column in columnProperties}}
        <td>data</td>
      {{/each}}
    </tr>
  {{/each}}
</tbody>

